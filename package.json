{
  "name": "adeo-test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "engines": {
    "node": ">=18.11.0"
  },
  "scripts": {
    "test": "jest --watchAll --silent --verbose",
    "start": "node --watch app.js --filter=ry --count"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
